# 기여 안내

이 저장소에 도움을 주시려는 분을 위한 안내입니다. 처음이어도 괜찮습니다.

## 1. 무엇을 도울 수 있나요

| 종류 | 예 |
|---|---|
| 오타·표현 | 문서의 오타, 어색한 문장 |
| 문서 | 설치·사용법에서 막힌 지점 보완 |
| 기능 | 새 명령, 오류 메시지 개선 |

## 2. 먼저 이슈를 올려 주세요

바로 고쳐 올리기 전에 **이슈**로 무엇을 바꾸려는지 한 줄 적어 주세요.
같은 일을 두 사람이 하지 않게 됩니다.

## 3. 가지(branch) 이름 규칙

```text
docs/사용법-오타
feat/명령-검색
fix/빈-목록-오류
```

## 4. 커밋 메시지 규칙

제목은 **50자 안쪽**, **무엇을 왜** 바꿨는지 적습니다.

```text
docs: 설치 안내에 파이썬 버전 확인 단계 추가

윈도우에서 python 이 아니라 py 로 실행되는 경우가 있어
버전 확인 명령을 먼저 하도록 순서를 바꿨다.
```

| 앞머리 | 언제 |
|---|---|
| `feat` | 기능을 더했을 때 |
| `fix` | 잘못된 동작을 고쳤을 때 |
| `docs` | 문서만 바꿨을 때 |
| `chore` | 설정·정리 |

## 5. PR 을 올릴 때

- 제목은 커밋 제목과 같게 씁니다
- 본문에 **무엇을 바꿨는지 세 줄**과 **확인한 방법**을 적습니다
- 문서를 바꿨다면 **화면 캡처 한 장**을 붙여 주세요

## 6. 검토

- 하루 안에 첫 답을 드립니다
- 고쳐 달라는 말은 **코드가 아니라 결과**에 대한 것입니다. 편하게 물어보셔도 됩니다

## 7. 문서 형식 규칙

문서 형식은 사람이 아니라 검사 도구가 봅니다. 저장소 맨 위의 `.pymarkdown` 파일이 규칙입니다.

```bash
pymarkdown scan docs
```

아무것도 안 나오고 종료 코드가 `0` 이면 통과입니다.

기본값에서 조정한 것은 아래 둘입니다.

- `md013` 줄 길이는 120 으로 둡니다 — 한국어는 한 글자가 넓어 80자가 지나치게 짧습니다
- `md036` 은 껐습니다 — 매뉴얼에서 굵은 글씨를 단계 제목으로 일부러 씁니다

출력을 보여 주는 코드 블록에는 `text` 를 붙입니다. 실행할 명령이 아니라는 표시입니다.
