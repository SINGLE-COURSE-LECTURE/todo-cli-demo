# 문서 사이트 운영

## 배포 뒤 점검

배포 이력에서 Active 가 붙은 커밋이 지금 보이는 사이트입니다.

- 시크릿 창으로 주소를 연다
- 목차와 검색이 되는지 본다
- 방금 고친 문장이 보이는지 본다
- 상태 배지가 둘 다 초록인지 본다

## 실패했을 때

실패해도 사이트는 직전에 배포된 내용으로 그대로 열립니다.

## 되돌리기

급하면 마지막 정상 배포를 재실행하고 이어서 저장소를 고칩니다.

```bash
git revert <문제 커밋>
```
